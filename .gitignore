# --- Gradle / build ---
.gradle/
build/
bridge-plugin/.gradle/
bridge-plugin/build/

# --- IntelliJ / editor ---
.idea/
*.iml
out/

# --- Python (host) ---
__pycache__/
*.pyc
.venv/
venv/
# runtime cache, not source (inline comments are NOT allowed on a pattern line)
host/tools_cache.json

# --- Local helper scripts ---
claude.bat

# --- VDI-tunnel routing-mode marker (runtime state, not source) ---
.claude/vdi-tunnel.active

# --- Notes about whatever VDI/project this checkout happens to be pointed at ---
# These differ per VDI and per target project; they are local scratch, not source.
docs/vdi-notes/

# --- OS cruft ---
.DS_Store
Thumbs.db

# NOTE: keep the private repo private — this is an in-house DLP-boundary tool.
