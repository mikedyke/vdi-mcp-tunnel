# The parity vectors are regenerated by host/tools/gen_parity_vectors.py, which writes
# LF. Pin the checkout to LF so regenerating on Windows is a no-op diff rather than a
# whole-file rewrite.
vectors/parity-vectors.txt text eol=lf
