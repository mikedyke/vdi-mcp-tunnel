// Standalone build on purpose: it must NOT apply the IntelliJ Platform plugin, so the
// parity test runs anywhere with a JDK + Gradle instead of pulling the ~1 GB IDE SDK.
rootProject.name = "codec-parity"
